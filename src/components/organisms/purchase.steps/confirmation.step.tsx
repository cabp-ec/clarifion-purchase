import React  from 'react';

interface ConfirmationStepProps {
}

export const ConfirmationStep: React.FC<ConfirmationStepProps> = (props: ConfirmationStepProps) => {
  return <div>
    CONFIRMATION
  </div>;
};
